# Level Requirements

Each level must include:
1. A `level.json` file containing level metadata (name, description)
2. A `config.json` file with level configuration settings

Example structure:
```
level-name/
  level.json
  config.json
```
